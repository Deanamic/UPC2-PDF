BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));
String[] tokens = reader.readLine().split("\\s+");
int zahl = Integer.parseInt(tokens[0]);
BigInteger big = new BigInteger(tokens[1]);
import java.awt.geom.*; // lines,points,segments,area,contains,intersect,ccw...
import java.util.*; // TreeSet, HashMap, HashSet, Date, Stack, ...
import java.util.Arrays / Collections; // sort,binarySearch,fill,min,max,reverse
import java.math.*; // BigInteger (mit gcd, modInverse,...), BigDecimal
import static java.lang.Math.*;//min,max,abs,cos,cosh,atan2,round,random,sqrt,..
import java.io.*; //BufferedReader,..
